#include <engine/gameObjects/FloorBox.hpp>

game::FloorBox::FloorBox(float top, float bottom, float left, float right) :
    top(top), bottom(bottom), left(left), right(right) {}

game::FloorBox::~FloorBox() {}
